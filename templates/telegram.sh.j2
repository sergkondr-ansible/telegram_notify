#!/bin/bash

CHAT_ID="{{ telegram_chat_id }}"
URL="https://api.telegram.org/bot{{telegram_bot_token}}/sendMessage"

if [[ "$#" -eq 0 ]]; then
    MESSAGE="Test"
else
    MESSAGE=$1
fi

curl -s -X POST $URL \
    -d chat_id=$CHAT_ID \
    -d text="$MESSAGE" \
>> /var/log/telegram.log
